# 🤖 Настройка Telegram Бота для AOneAgency.kz

## Что это дает?

Все заявки с сайта (формы и квизы) будут автоматически приходить в ваш Telegram чат в реальном времени.

## 📝 Пошаговая инструкция

### Шаг 1: Создание бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Введите имя бота (например, "AOneAgency Notifications")
4. Введите username бота (например, "aoneagency_bot")
5. **Сохраните токен**, который выдаст BotFather (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получение Chat ID

#### Вариант А: Личный чат (простой)

1. Найдите своего бота в Telegram по username
2. Нажмите **Start** или отправьте любое сообщение
3. Найдите бота **@userinfobot** или **@RawDataBot**
4. Отправьте ему команду `/start`
5. Скопируйте ваш **ID** (это будет число, например: `123456789`)

#### Вариант Б: Групповой чат (для команды)

1. Создайте группу в Telegram
2. Добавьте своего бота в группу (через "Add Members")
3. Сделайте бота **администратором** группы
4. Отправьте в группу любое сообщение
5. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Замените `<ВАШ_ТОКЕН>` на токен из Шага 1
6. Найдите в ответе `"chat":{"id":-123456789` — это ваш Chat ID
   - Для групп ID начинается с минуса!

### Шаг 3: Настройка проекта

1. Создайте файл `.env.local` в корне проекта (если его нет)
2. Добавьте переменные:

```bash
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

**Важно:**
- Если используете группу, Chat ID будет с минусом: `-123456789`
- Не добавляйте `.env.local` в git! (уже в `.gitignore`)

### Шаг 4: Проверка работы

Запустите тестовый скрипт:

```bash
npm run test:telegram
```

Если всё настроено правильно, в Telegram придут 3 тестовых сообщения:
1. Простое тестовое сообщение
2. Пример заявки с формы контактов
3. Пример прохождения квиза

## 🎯 Что будет приходить в Telegram?

### Заявка с формы контактов:

```
🆕 Новая заявка с сайта!

👤 Имя: Иван Иванов
📧 Email: ivan@example.com
📞 Телефон: +7 747 385 4493
💬 Сообщение:
Интересует продвижение в Instagram

📅 Дата: 20.11.2024, 15:30:45
```

### Прохождение квиза:

```
🎯 Новый результат квиза!

👤 Имя: Иван Иванов
📞 Телефон: +7 747 385 4493
📧 Email: ivan@example.com

Ответы:
• Цель: Увеличение заявок
• Ниша: E-commerce
• Бюджет: От 200,000 ₸/месяц
• Платформы: Instagram, TikTok

💰 Примерный бюджет: От 200,000 ₸/месяц

📅 Дата: 20.11.2024, 15:35:20
```

## 🔧 Дополнительные настройки

### Отправка в несколько чатов

Можно отправлять уведомления в разные чаты:

```typescript
// В коде API route
import { sendToTelegram, formatContactMessage } from '@/lib/telegram';

// Отправка в основной чат
await sendToTelegram(message);

// Отправка в дополнительный чат
await sendToTelegram(message, process.env.TELEGRAM_SALES_CHAT_ID);
```

### Отключение уведомлений на ночь

Добавьте в `.env.local`:

```bash
TELEGRAM_QUIET_HOURS_START=23
TELEGRAM_QUIET_HOURS_END=9
```

И обновите `lib/telegram.ts`:

```typescript
const hour = new Date().getHours();
const quietStart = Number.parseInt(process.env.TELEGRAM_QUIET_HOURS_START || '0');
const quietEnd = Number.parseInt(process.env.TELEGRAM_QUIET_HOURS_END || '0');

if (hour >= quietStart || hour < quietEnd) {
  // Отправляем без звука
  telegramMessage.disable_notification = true;
}
```

## 🚨 Решение проблем

### Ошибка "Bot token не настроен"

- Проверьте, что файл `.env.local` находится в корне проекта
- Проверьте, что переменная называется именно `TELEGRAM_BOT_TOKEN`
- Перезапустите dev-сервер после добавления переменных

### Ошибка "Chat not found"

- Убедитесь, что бот добавлен в чат
- Для групп: убедитесь, что бот является администратором
- Проверьте, что Chat ID правильный (с минусом для групп)

### Сообщения не приходят

- Запустите `npm run test:telegram` для диагностики
- Проверьте логи сервера на наличие ошибок
- Убедитесь, что переменные окружения загружены

## 📊 Мониторинг

Все отправки логируются в консоль сервера. При ошибке отправки в Telegram:
- Заявка всё равно сохраняется в `data/contact-submissions.json` или `data/quiz-submissions.json`
- Пользователь получает успешный ответ
- Ошибка логируется для дебага

## 🔒 Безопасность

- ✅ Токен бота хранится в `.env.local` (не в git)
- ✅ Сообщения экранируются от HTML-инъекций
- ✅ Отправка не блокирует обработку заявки
- ✅ Ошибки Telegram не влияют на работу сайта

## 📱 Рекомендации

1. **Используйте группу** вместо личного чата для команды
2. **Настройте уведомления** в Telegram (чтобы не пропустить заявки)
3. **Добавьте быстрые ответы** в Telegram для типовых вопросов
4. **Интегрируйте с CRM** (amoCRM) для автоматизации

## 🎉 Готово!

Теперь все заявки с сайта будут автоматически приходить в Telegram!

