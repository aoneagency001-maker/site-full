# Руководство по разработке

> Рекомендации и best practices для разработки проекта AOneAgency.kz

## 📁 Структура проекта

```
agency-kit-site/
├── app/                          # Next.js App Router (основа приложения)
│   ├── [locale]/                # Мультиязычные страницы
│   │   ├── page.tsx            # Главная страница
│   │   ├── about/              # О компании
│   │   ├── blog/               # Блог
│   │   ├── contacts/           # Контакты
│   │   └── [услуги]/           # Страницы услуг
│   ├── api/                     # API Routes
│   │   ├── contact/            # API контактной формы
│   │   ├── quiz/               # API квиза
│   │   ├── track-visitor/      # API отслеживания посетителей
│   │   └── analytics/          # API аналитики Яндекс
│   └── layout.tsx              # Корневой layout
├── components/                   # React компоненты
│   ├── custom/                 # Кастомные компоненты
│   │   ├── Navbar.tsx          # Навигация
│   │   ├── Footer.tsx          # Подвал
│   │   └── ...                 # Другие компоненты
│   ├── landing/                # Компоненты лендинга
│   │   ├── HeroSection.tsx     # Главный экран
│   │   ├── ServicesSection.tsx # Секция услуг
│   │   └── ...                 # Другие секции
│   ├── quiz/                   # Компоненты квиза
│   └── ui/                     # UI компоненты (shadcn/ui)
├── lib/                         # Утилиты и библиотеки
│   ├── utils.ts                # Общие утилиты
│   ├── telegram.ts             # Telegram интеграция
│   ├── blog.ts                 # Работа с блогом
│   └── ...                     # Другие утилиты
├── messages/                    # i18n переводы
│   ├── ru.json                 # Русский (основной)
│   ├── kk.json                 # Казахский
│   └── en.json                 # Английский
├── data/                        # Данные (в .gitignore)
│   ├── blogData.ts             # Данные блога
│   ├── caseStudies.ts          # Кейсы
│   └── *.json                  # Пользовательские данные
├── docs/                        # Документация (организована по версиям)
│   ├── v01/                    # Версия 1
│   ├── v02/                    # Версия 2
│   ├── v03/                    # Версия 3
│   ├── v04/                    # Версия 4
│   └── general/                # Общая документация
├── scripts/                     # Служебные скрипты
│   ├── test-telegram.ts        # Тестирование Telegram
│   ├── test-visitor-tracking.ts # Тестирование отслеживания
│   └── ...                     # Другие скрипты
└── public/                      # Статические файлы
    ├── robots.txt              # SEO
    └── favicon.ico             # Фавикон
```

## 🛠️ Технологический стек

### Основа
- **Next.js 15.5.2** - React фреймворк с App Router
- **React 19** - UI библиотека
- **TypeScript 5** - Типизация

### Стилизация
- **Tailwind CSS 4** - Utility-first CSS
- **shadcn/ui** - Компоненты на базе Radix UI
- **GSAP** - Анимации

### Интернационализация
- **next-intl** - Мультиязычность (ru/kk/en)

### Инструменты
- **Biome** - Линтер и форматтер
- **Bun/npm** - Пакетный менеджер

## 🚀 Рабочий процесс

### 1. Начало работы

```bash
# Установка зависимостей
npm install

# Настройка переменных окружения
cp docs/general/ENVIRONMENT-VARIABLES.md .env.local
# Заполните .env.local реальными значениями

# Запуск dev-сервера
npm run dev
```

### 2. Разработка компонентов

**Правила:**
- Используйте TypeScript для всех компонентов
- Следуйте именованию: `PascalCase` для компонентов
- Используйте `'use client'` только когда необходимо
- Предпочитайте Server Components когда возможно

**Пример компонента:**

```typescript
// components/custom/MyComponent.tsx
'use client'; // только если нужна клиентская логика

interface MyComponentProps {
  title: string;
  description?: string;
}

export function MyComponent({ title, description }: MyComponentProps) {
  return (
    <div className="container mx-auto">
      <h2 className="text-2xl font-bold">{title}</h2>
      {description && <p className="text-muted-foreground">{description}</p>}
    </div>
  );
}
```

### 3. Работа с переводами

**Добавление нового текста:**

1. Добавьте ключ во все файлы переводов:

```json
// messages/ru.json
{
  "hero": {
    "title": "Ваш заголовок"
  }
}

// messages/kk.json
{
  "hero": {
    "title": "Сіздің тақырыбыңыз"
  }
}

// messages/en.json
{
  "hero": {
    "title": "Your Title"
  }
}
```

2. Используйте в компоненте:

```typescript
import { useTranslations } from 'next-intl';

export function Hero() {
  const t = useTranslations('hero');
  return <h1>{t('title')}</h1>;
}
```

### 4. Создание новых страниц

Создавайте страницы в `app/[locale]/`:

```typescript
// app/[locale]/new-page/page.tsx
import { useTranslations } from 'next-intl';

export default function NewPage() {
  const t = useTranslations('newPage');
  
  return (
    <main className="container mx-auto py-8">
      <h1>{t('title')}</h1>
      {/* Ваш контент */}
    </main>
  );
}
```

### 5. API Routes

**Структура API Route:**

```typescript
// app/api/my-endpoint/route.ts
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    
    // Ваша логика
    
    return NextResponse.json({ success: true, data: {} });
  } catch (error) {
    return NextResponse.json(
      { success: false, error: 'Error message' },
      { status: 500 }
    );
  }
}
```

## 📝 Код-стайл

### TypeScript

```typescript
// ✅ Хорошо
interface User {
  name: string;
  email: string;
}

export function greetUser(user: User): string {
  return `Hello, ${user.name}!`;
}

// ❌ Плохо
function greet(user: any) {
  return "Hello, " + user.name;
}
```

### React компоненты

```typescript
// ✅ Хорошо - деструктуризация пропсов, типизация
interface ButtonProps {
  label: string;
  onClick: () => void;
  variant?: 'primary' | 'secondary';
}

export function Button({ label, onClick, variant = 'primary' }: ButtonProps) {
  return (
    <button 
      onClick={onClick}
      className={`btn btn-${variant}`}
    >
      {label}
    </button>
  );
}

// ❌ Плохо - нет типов, props через props.*
export function Button(props) {
  return <button onClick={props.onClick}>{props.label}</button>;
}
```

### CSS/Tailwind

```typescript
// ✅ Хорошо - семантические классы, группировка
<div className="container mx-auto px-4 py-8 bg-white rounded-lg shadow-md">
  <h1 className="text-2xl font-bold text-gray-900">Title</h1>
</div>

// ❌ Плохо - нечитаемая куча классов
<div className="w-full max-w-7xl mx-auto p-4 pt-8 pb-8 bg-white rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
```

## ✅ Чек-лист перед коммитом

- [ ] Код отформатирован: `npm run format`
- [ ] Нет ошибок линтера: `npm run lint`
- [ ] Типы корректны: `npm run build`
- [ ] Добавлены переводы для всех 3 языков
- [ ] Нет хардкоженных токенов и паролей
- [ ] Новые файлы документированы
- [ ] Протестировано в браузере

## 🧪 Тестирование

```bash
# Тестирование Telegram
npm run test:telegram

# Тестирование отслеживания посетителей
npx tsx scripts/test-visitor-tracking.ts

# Проверка сборки
npm run build

# Запуск production сервера
npm run start
```

## 📦 Добавление зависимостей

```bash
# Установка библиотеки
npm install package-name

# Установка dev-зависимости
npm install -D package-name

# Обновление всех зависимостей
npm update
```

**Перед добавлением новой зависимости:**
1. Проверьте, нет ли уже аналога в проекте
2. Убедитесь, что библиотека поддерживается
3. Проверьте размер бандла
4. Документируйте зачем она нужна

## 📚 Создание документации

### Именование файлов

```
vXX_YYYY-MM-DD_HH-MM-SS_НАЗВАНИЕ.md
```

Пример: `v05_2025-11-21_12-30-00_NEW-FEATURE.md`

### Размещение

- **Версионная документация**: `docs/vXX/`
- **Общая документация**: `docs/general/`

### Шаблон документа

```markdown
# Название документа

> Краткое описание

## Цель

Зачем этот документ существует

## Инструкции

Пошаговые инструкции

## Примеры

Примеры использования

## Troubleshooting

Решение проблем

---

Дата: DD.MM.YYYY
```

## 🔒 Безопасность

### Что НЕ коммитить

- ❌ `.env.local`
- ❌ `data/*.json` (пользовательские данные)
- ❌ API токены и ключи
- ❌ Пароли и секреты
- ❌ Личные данные пользователей

### Проверка перед коммитом

```bash
# Проверьте, что файлы игнорируются
git status

# Убедитесь, что .env.local не в списке
```

## 🐛 Отладка

### Логирование

```typescript
// Development
if (process.env.NODE_ENV === 'development') {
  console.log('Debug info:', data);
}

// Production - используйте правильное логирование
```

### Инструменты

- React DevTools
- Next.js DevTools
- Browser DevTools
- Yandex.Metrika для аналитики

## 📖 Полезные ссылки

- [Next.js Docs](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [shadcn/ui](https://ui.shadcn.com/)
- [next-intl](https://next-intl-docs.vercel.app/)
- [Biome](https://biomejs.dev/)

---

**Помните:** Код пишется один раз, но читается много раз. Пишите чистый, понятный код!

Дата создания: 20.11.2025  
Последнее обновление: 20.11.2025

