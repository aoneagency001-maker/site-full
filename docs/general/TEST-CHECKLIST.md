# ✅ Чеклист для тестирования на localhost

## 🚀 Сервер запущен

Сервер должен быть доступен на: `http://localhost:3000`

## 📋 Пошаговая проверка

### ✅ Шаг 1: Проверка тестового endpoint (30 секунд)

Откройте в браузере:
```
http://localhost:3000/api/track-visitor/test
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "telegram": {
    "hasToken": true,    ← должно быть true
    "hasChatId": true,   ← должно быть true
    ...
  }
}
```

**Если `false`:**
- Проверьте файл `.env.local`
- Убедитесь, что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` установлены
- Перезапустите сервер

---

### ✅ Шаг 2: Открытие сайта (1 минута)

1. Откройте: `http://localhost:3000`
2. Нажмите **F12** → вкладка **Console**
3. Найдите логи, начинающиеся с `[VISITOR-TRACKER]`

**Должно быть:**
```
[VISITOR-TRACKER] 🚀 Starting tracking for pathname: /ru
[VISITOR-TRACKER] 📤 Sending tracking request: {page: "/ru", sessionId: "...", referrer: "..."}
[VISITOR-TRACKER] 📥 Response status: 200
[VISITOR-TRACKER] 📦 Response data: {tracked: true, visitorId: "..."}
[VISITOR-TRACKER] ✅ Visitor tracked successfully: ...
```

**Если видите ошибки:**
- `Failed to fetch` → сервер не запущен или недоступен
- `404` → проблема с маршрутизацией
- `500` → ошибка на сервере (смотрите логи в терминале)

---

### ✅ Шаг 3: Проверка логов сервера (1 минута)

В терминале, где запущен `npm run dev`, должны быть логи:

**Успешно:**
```
[TRACK-xxx] 📥 New tracking request received
[TRACK-xxx] Page: /ru
[TRACK-xxx] ✅ Request passed validation, processing...
[TRACK-xxx] 💾 Saving visitor data: ...
[TRACK-xxx] ✅ Visitor data saved
[TRACK-xxx] 📤 Preparing Telegram notification (first visit)...
[TELEGRAM] 📤 Sending notification for visitor: ...
[TELEGRAM] ✅ Notification sent successfully for visitor: ...
[TRACK-xxx] ✅ Visitor tracked successfully: ...
```

**Проблемы:**
- `❌ Request rejected: bot detected` → посетитель определяется как бот
- `❌ Request rejected: invalid IP` → проблема с IP (редко)
- `Telegram credentials not configured` → не установлены переменные окружения
- `❌ Failed to send notification` → проблема с Telegram API

---

### ✅ Шаг 4: Проверка Telegram (30 секунд)

Проверьте Telegram - должно прийти уведомление:

**Первое посещение:**
```
🆕 Новый посетитель на сайте!

👤 Информация:
📍 Местоположение: ...
🌐 IP: ...
💻 Устройство: ...
...
```

**Последующие посещения:**
```
🔄 Посетитель вернулся на сайт!

👤 Информация:
...
🔄 Это повторное посещение в этой сессии
```

**Если уведомление не пришло:**
1. Проверьте логи сервера на ошибки `[TELEGRAM]`
2. Проверьте переменные окружения
3. Проверьте, что бот не заблокирован

---

### ✅ Шаг 5: Тестирование с разных страниц (1 минута)

1. Перейдите на другую страницу (например, `/ru/about`)
2. Проверьте консоль браузера - должно быть новое уведомление
3. Проверьте Telegram - должно прийти уведомление "Посетитель вернулся"

---

### ✅ Шаг 6: Проверка сохраненных данных (30 секунд)

Проверьте файл:
```
data/visitors.json
```

Должны быть сохранены данные о посетителях в формате JSON.

---

## 🎯 Итоговый чеклист

- [ ] Тестовый endpoint `/api/track-visitor/test` отвечает и показывает `hasToken: true, hasChatId: true`
- [ ] В консоли браузера видны логи `[VISITOR-TRACKER]` с успешным статусом
- [ ] В логах сервера видны логи `[TRACK-xxx]` и `[TELEGRAM]` без ошибок
- [ ] В Telegram приходят уведомления о посетителях
- [ ] При переходе на другую страницу приходит новое уведомление
- [ ] Данные сохраняются в `data/visitors.json`

## ✅ Если все работает

Можно пушить на GitHub! 🚀

```bash
git add .
git commit -m "Fix: Улучшена система отслеживания посетителей с уведомлениями в Telegram"
git push
```

## ❌ Если что-то не работает

1. Проверьте логи в консоли браузера
2. Проверьте логи сервера в терминале
3. Проверьте переменные окружения в `.env.local`
4. Смотрите документацию в `DIAGNOSTICS.md` и `QUICK-FIX.md`

