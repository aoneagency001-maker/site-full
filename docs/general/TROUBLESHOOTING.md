# 🔧 Troubleshooting - Решение проблем

## ❌ Проблема: `/api/telegram-check` показывает `configured: false`

### Причина
Переменные окружения `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` **не установлены на production**.

### ✅ Решение

1. **Установите переменные на Vercel:**
   - Dashboard → Проект → Settings → Environment Variables
   - Добавьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
   - Выберите **все окружения** (Production, Preview, Development)
   - **ВАЖНО:** Передеплойте проект после добавления!

2. **Проверьте:**
   - Откройте: `https://aoneagency.kz/api/telegram-check`
   - Должно быть: `"configured": true`

---

## ❌ Проблема: Уведомления не приходят в Telegram

### Шаг 1: Проверьте переменные окружения

```bash
# Откройте в браузере
https://aoneagency.kz/api/telegram-check
```

Если `configured: false` → см. решение выше.

### Шаг 2: Проверьте логи на Vercel

1. Vercel Dashboard → Проект → **Logs** → **Runtime Logs**
2. Ищите сообщения:
   - `[TELEGRAM] ✅ Notification sent successfully` → работает ✅
   - `[TELEGRAM] ❌ Failed to send notification` → ошибка отправки
   - `[TELEGRAM] ⚠️ WARNING: Telegram credentials not configured!` → переменные не установлены

### Шаг 3: Проверьте токен и Chat ID

**Токен должен быть:**
- Формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
- Начинается с цифр
- Содержит двоеточие

**Chat ID должен быть:**
- Число (например: `123456789`)
- Вы должны написать боту хотя бы одно сообщение

### Шаг 4: Проверьте, что это первое посещение в сессии

Уведомления отправляются **только при первом посещении в сессии**.

- Откройте сайт в **режиме инкогнито** (новое посещение)
- Должно прийти уведомление ✅
- Перейдите на другую страницу → уведомление **не должно** прийти (это нормально)

---

## ❌ Проблема: Ошибка в консоли браузера

### Ошибка: `Failed to load resource: 404`

**Причина:** API route не найден или неправильный путь.

**Решение:**
- Проверьте, что сайт задеплоен
- Проверьте путь: `/api/track-visitor`

### Ошибка: `Failed to fetch`

**Причина:** Проблема с сетью или CORS.

**Решение:**
- Проверьте, что сайт доступен
- Проверьте логи сервера

---

## ❌ Проблема: Посетители определяются как боты

**Признаки:**
- В логах: `[TRACK-xxx] ❌ Request rejected: bot detected`
- Уведомления не приходят

**Решение:**
- Проверьте User-Agent в логах
- Если это реальный браузер, но определяется как бот → проблема в списке ботов
- Можно временно отключить проверку для тестирования

---

## ❌ Проблема: "Посетитель не найден" (404)

**Причина:** На production файловая система read-only, данные не сохраняются.

**Решение:**
- Это **не критично** - уведомления все равно отправляются в Telegram
- Ошибка исправлена - теперь возвращается статус 200 вместо 404

---

## 📋 Чеклист диагностики

- [ ] Переменные окружения установлены на Vercel
- [ ] Проект передеплоен после установки переменных
- [ ] `/api/telegram-check` показывает `configured: true`
- [ ] Токен бота правильного формата
- [ ] Chat ID установлен и вы написали боту
- [ ] Открываете сайт в режиме инкогнито (новое посещение)
- [ ] Проверяете логи на Vercel
- [ ] Бот не заблокирован в Telegram

---

## 🆘 Если ничего не помогает

1. **Проверьте логи на Vercel:**
   - Dashboard → Logs → Runtime Logs
   - Ищите все сообщения с `[TELEGRAM]` или `[TRACK]`

2. **Проверьте токен:**
   - Откройте: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe`
   - Должен вернуться JSON с информацией о боте

3. **Проверьте Chat ID:**
   - Запустите: `npx tsx scripts/get-telegram-chat-id.ts`
   - Убедитесь, что вы написали боту

4. **Создайте тестовое уведомление:**
   - Запустите: `npx tsx scripts/send-test-notification.ts`
   - Если работает → проблема в логике отслеживания
   - Если не работает → проблема в токене/Chat ID

---

**Дата:** 20.11.2025


