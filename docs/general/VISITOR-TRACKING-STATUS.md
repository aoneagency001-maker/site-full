# 📊 Статус системы отслеживания посетителей и уведомлений в Telegram

## ✅ Что было исправлено

### 1. **Логика определения ботов**
- ❌ **Было**: Список ботов включал "http", что могло ловить нормальные браузеры
- ✅ **Исправлено**: Исключен "http" из проверки, добавлено подробное логирование

### 2. **Проверка IP-адресов**
- ❌ **Было**: IP "unknown" или приватные IP блокировали отслеживание
- ✅ **Исправлено**: Разрешены "unknown" IP (для прокси/CDN) и приватные IP (для разработки)

### 3. **Отслеживание при навигации**
- ❌ **Было**: VisitorTracker отправлял запрос только при первой загрузке страницы
- ✅ **Исправлено**: Теперь отслеживает изменения пути (pathname) и отправляет запрос при каждой смене страницы

### 4. **Логика отправки уведомлений**
- ✅ **Текущая логика**: Уведомления отправляются при каждом посещении сайта
- ✅ **Определение типа посещения**: Система различает первое посещение и последующие (для статистики)
- ✅ **Сообщения в Telegram**: Указывается, первое это посещение или повторное

### 5. **Логирование для диагностики**
- ✅ Добавлено подробное логирование на всех этапах:
  - В VisitorTracker (клиентская сторона)
  - В API route (серверная сторона)
  - При отправке в Telegram

## 🔍 Как проверить работу системы

### 1. Проверка в браузере (консоль разработчика)
Откройте консоль браузера (F12) и проверьте логи:
- `[VISITOR-TRACKER]` - логи с клиентской стороны
- Должны быть сообщения о отправке запросов и успешном отслеживании

### 2. Проверка на сервере (логи)
Проверьте логи сервера на наличие:
- `[TRACK]` - логи обработки запросов
- `[TELEGRAM]` - логи отправки уведомлений

### 3. Тестовый скрипт
Запустите тестовый скрипт:
```bash
npx tsx scripts/test-visitor-tracking.ts
```

## 📋 Текущий статус компонентов

### ✅ Работает
- **VisitorTracker компонент**: Подключен в layout, отслеживает изменения пути
- **API route `/api/track-visitor`**: Принимает запросы, фильтрует ботов, сохраняет данные
- **Отправка в Telegram**: Настроена, отправляет уведомления при первом посещении
- **Отслеживание форм**: Уведомления приходят при заполнении форм контактов
- **Отслеживание квизов**: Уведомления приходят при прохождении квизов

### ⚠️ Требует проверки
- **Переменные окружения**: Убедитесь, что установлены:
  - `TELEGRAM_BOT_TOKEN`
  - `TELEGRAM_CHAT_ID`
- **Геолокация**: Зависит от внешнего сервиса ip-api.com
- **Фильтрация ботов**: Может блокировать некоторых реальных посетителей (проверьте логи)

## 🐛 Возможные проблемы и решения

### Проблема: Уведомления не приходят
**Решение:**
1. Проверьте переменные окружения `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
2. Проверьте логи сервера на наличие ошибок
3. Убедитесь, что это первое посещение сайта (очистите sessionStorage)

### Проблема: Посетители определяются как боты
**Решение:**
1. Проверьте логи `[TRACK] Bot detected:` в консоли сервера
2. Если реальный посетитель определяется как бот, добавьте исключение в список `BOT_USER_AGENTS`

### Проблема: Запросы не отправляются
**Решение:**
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что компонент VisitorTracker подключен в layout
3. Проверьте, что путь изменяется при навигации (для Next.js App Router)

## 📝 Что делать дальше

1. **Протестируйте систему:**
   - Откройте сайт в режиме инкогнито (чтобы это было первое посещение)
   - Проверьте, пришло ли уведомление в Telegram с пометкой "🆕 Новый посетитель"
   - Перейдите на другую страницу - должно прийти уведомление с пометкой "🔄 Посетитель вернулся"

2. **Проверьте логи:**
   - В консоли браузера должны быть логи `[VISITOR-TRACKER]`
   - В логах сервера должны быть логи `[TRACK]` и `[TELEGRAM]`

3. **Мониторинг:**
   - Следите за логами в течение нескольких дней
   - Проверяйте, что уведомления приходят для реальных посетителей
   - Убедитесь, что боты не отправляют уведомления

## 🔧 Технические детали

### Структура запроса
```typescript
{
  page: string,              // Текущая страница
  landingPage: string,       // Первая страница в сессии
  referrer: string | null,   // Откуда пришел посетитель
  screenResolution: string,  // Разрешение экрана
  sessionId: string,         // ID сессии
  isFirstVisit: boolean,     // Флаг первого посещения
  utmSource?: string,        // UTM метки
  ...
}
```

### Условия отправки уведомления
1. ✅ Посетитель не является ботом
2. ✅ IP адрес валидный (или unknown)
3. ✅ Уведомление отправляется при каждом посещении (и при первом, и при последующих)
4. ✅ Настроены переменные окружения Telegram
5. ✅ Система определяет тип посещения (`isFirstVisit`) для статистики и отображения в сообщении

### Логика работы
1. При загрузке страницы VisitorTracker отправляет запрос на `/api/track-visitor`
2. API проверяет, является ли посетитель ботом
3. API получает геолокацию по IP
4. API сохраняет данные в файл `data/visitors.json`
5. API отправляет уведомление в Telegram при каждом посещении
6. В сообщении указывается, первое это посещение (`🆕 Новый посетитель`) или повторное (`🔄 Посетитель вернулся`)
7. При переходе на другую страницу процесс повторяется с соответствующим типом посещения

---

**Дата проверки:** $(date)
**Версия:** 2.0
**Статус:** ✅ Исправлено и готово к тестированию

