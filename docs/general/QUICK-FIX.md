# 🔧 Быстрая диагностика проблемы с уведомлениями

## ⚡ Быстрая проверка (5 минут)

### 1. Проверьте тестовый endpoint

Откройте в браузере:
```
https://ваш-сайт.com/api/track-visitor/test
```

**Должно вернуться:**
```json
{
  "status": "ok",
  "telegram": {
    "hasToken": true,
    "hasChatId": true,
    ...
  }
}
```

**Если `hasToken: false` или `hasChatId: false`** - проблема в переменных окружения!

### 2. Проверьте консоль браузера

1. Откройте сайт
2. Нажмите F12 → Console
3. Найдите логи `[VISITOR-TRACKER]`

**Должно быть:**
```
[VISITOR-TRACKER] 🚀 Starting tracking for pathname: /ru
[VISITOR-TRACKER] 📤 Sending tracking request: ...
[VISITOR-TRACKER] 📥 Response status: 200
[VISITOR-TRACKER] ✅ Visitor tracked successfully: ...
```

**Если видите ошибки:**
- `Failed to fetch` → API недоступен
- `404` → API route не найден  
- `500` → Ошибка на сервере

### 3. Проверьте логи сервера

Найдите логи, начинающиеся с `[TRACK-`:

**Успешно:**
```
[TRACK-xxx] 📥 New tracking request received
[TRACK-xxx] ✅ Request passed validation
[TRACK-xxx] 📤 Preparing Telegram notification
[TELEGRAM] ✅ Notification sent successfully
```

**Проблемы:**
- `❌ Request rejected: bot detected` → Посетитель определяется как бот
- `❌ Request rejected: invalid IP` → Проблема с IP
- `Telegram credentials not configured` → Не установлены переменные окружения
- `❌ Failed to send notification` → Проблема с Telegram API

## 🎯 Самые частые проблемы

### Проблема 1: Переменные окружения не установлены

**Решение:**
1. Проверьте файл `.env.local` или настройки на сервере
2. Убедитесь, что есть:
   - `TELEGRAM_BOT_TOKEN=ваш_токен`
   - `TELEGRAM_CHAT_ID=ваш_chat_id`
3. Перезапустите сервер после изменения

### Проблема 2: Посетители определяются как боты

**Решение:**
1. Проверьте логи: `[TRACK-xxx] ❌ Request rejected: bot detected`
2. Если это реальный браузер, но определяется как бот - нужно исправить список ботов
3. Временно можно отключить проверку для тестирования

### Проблема 3: Запросы не доходят до API

**Решение:**
1. Проверьте, что сайт работает
2. Проверьте, что API доступен: `https://ваш-сайт.com/api/track-visitor/test`
3. Проверьте CORS настройки (если есть)

## 📝 Что делать дальше

1. **Соберите логи:**
   - Из консоли браузера (F12 → Console)
   - С сервера (логи приложения)

2. **Проверьте тестовый endpoint:**
   - Откройте `/api/track-visitor/test`
   - Убедитесь, что переменные окружения установлены

3. **Проверьте с разных устройств:**
   - С телефона
   - С компьютера
   - В режиме инкогнито

4. **Если ничего не помогает:**
   - Смотрите полную документацию в `DIAGNOSTICS.md`
   - Проверьте все логи внимательно
   - Убедитесь, что переменные окружения правильные

