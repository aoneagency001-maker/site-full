# Переменные окружения

> Документация по настройке переменных окружения для проекта AOneAgency.kz

## 📋 Обязательные переменные

### NEXT_PUBLIC_SITE_URL
URL вашего сайта (обязательно для production)

```env
NEXT_PUBLIC_SITE_URL=https://aoneagency.kz
```

### TELEGRAM_BOT_TOKEN
Токен Telegram бота для отправки уведомлений

**Как получить:**
1. Напишите @BotFather в Telegram
2. Создайте нового бота командой `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### TELEGRAM_CHAT_ID
ID чата для получения уведомлений

**Как получить:**
1. Напишите вашему боту любое сообщение (например `/start`)
2. Запустите скрипт: `npx tsx scripts/get-telegram-chat-id.ts`
3. Скопируйте полученный Chat ID

```env
TELEGRAM_CHAT_ID=123456789
```

## 🔧 Опциональные переменные

### NEXT_PUBLIC_YM_ID
ID счетчика Yandex.Metrika для аналитики

```env
NEXT_PUBLIC_YM_ID=12345678
```

### YANDEX_METRIKA_OAUTH_TOKEN
OAuth токен для API Yandex.Metrika (для получения отчетов через API)

**Как получить:**
1. Перейдите на [OAuth Яндекса](https://oauth.yandex.ru/)
2. Зарегистрируйте приложение
3. Получите токен с правами на чтение статистики

```env
YANDEX_METRIKA_OAUTH_TOKEN=your_oauth_token_here
```

## 📝 Создание .env.local

1. Создайте файл `.env.local` в корне проекта
2. Скопируйте и заполните необходимые переменные:

```env
# ОСНОВНЫЕ НАСТРОЙКИ
NEXT_PUBLIC_SITE_URL=https://aoneagency.kz

# TELEGRAM
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# YANDEX.METRIKA (опционально)
NEXT_PUBLIC_YM_ID=your_metrika_id
YANDEX_METRIKA_OAUTH_TOKEN=your_oauth_token
```

## 🔒 Безопасность

⚠️ **ВАЖНО:**

- Никогда не коммитьте `.env.local` в git!
- Файл `.env.local` уже добавлен в `.gitignore`
- Не храните реальные токены в коде
- Для production используйте переменные окружения платформы (Vercel, DigitalOcean и т.д.)

## ✅ Проверка настройки

### Тестирование Telegram

```bash
npm run test:telegram
```

Этот скрипт проверит:
- ✅ Наличие `TELEGRAM_BOT_TOKEN`
- ✅ Наличие `TELEGRAM_CHAT_ID`
- ✅ Отправку простого сообщения
- ✅ Отправку сообщения о заявке
- ✅ Отправку сообщения о квизе

### Тестирование отслеживания посетителей

```bash
npx tsx scripts/test-visitor-tracking.ts
```

### Отправка тестового уведомления

```bash
npx tsx scripts/send-test-notification.ts
```

## 📚 Дополнительные ресурсы

- [Next.js Environment Variables](https://nextjs.org/docs/basic-features/environment-variables)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Yandex.Metrika API](https://yandex.ru/dev/metrika/)

## 🆘 Troubleshooting

### Ошибка: "TELEGRAM_BOT_TOKEN не установлен"

Убедитесь, что:
1. Файл `.env.local` существует в корне проекта
2. Переменная `TELEGRAM_BOT_TOKEN` правильно прописана
3. Нет пробелов вокруг знака `=`
4. Перезапустите dev-сервер после изменения `.env.local`

### Ошибка: "Unauthorized" при отправке в Telegram

Проверьте:
1. Токен бота скопирован полностью и правильно
2. Бот не был удален в @BotFather
3. Вы отправили хотя бы одно сообщение боту

### Сообщения не приходят в Telegram

1. Проверьте Chat ID командой: `npx tsx scripts/get-telegram-chat-id.ts`
2. Убедитесь, что вы написали боту хотя бы одно сообщение
3. Проверьте, что бот не заблокирован

---

Дата создания: 20.11.2025  
Последнее обновление: 20.11.2025


