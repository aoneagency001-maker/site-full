# 🧪 Локальное тестирование системы отслеживания

## 📋 Шаг 1: Проверка переменных окружения

Создайте файл `.env.local` в корне проекта (если его еще нет):

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id

# Site URL (для локального тестирования)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Опционально (если используете)
NEXT_PUBLIC_GA_ID=ваш_ga_id
NEXT_PUBLIC_YM_ID=ваш_ym_id
```

**Важно:** Файл `.env.local` уже в `.gitignore`, он не попадет в Git.

## 🚀 Шаг 2: Установка зависимостей

```bash
cd "/Users/vbut/Сайт aoneagency.kz/agency-kit-site"
npm install
```

## 🏃 Шаг 3: Запуск локального сервера

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

## ✅ Шаг 4: Тестирование

### 4.1. Проверка тестового endpoint

Откройте в браузере:
```
http://localhost:3000/api/track-visitor/test
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "telegram": {
    "hasToken": true,
    "hasChatId": true,
    ...
  }
}
```

Если `hasToken: false` или `hasChatId: false` - проверьте `.env.local`

### 4.2. Проверка отслеживания посетителей

1. Откройте сайт: `http://localhost:3000`
2. Откройте консоль браузера (F12 → Console)
3. Найдите логи `[VISITOR-TRACKER]`

**Должно быть:**
```
[VISITOR-TRACKER] 🚀 Starting tracking for pathname: /ru
[VISITOR-TRACKER] 📤 Sending tracking request: {page: "/ru", ...}
[VISITOR-TRACKER] 📥 Response status: 200
[VISITOR-TRACKER] ✅ Visitor tracked successfully: ...
```

### 4.3. Проверка логов сервера

В терминале, где запущен `npm run dev`, должны быть логи:

```
[TRACK-xxx] 📥 New tracking request received
[TRACK-xxx] ✅ Request passed validation, processing...
[TRACK-xxx] 💾 Saving visitor data: ...
[TRACK-xxx] 📤 Preparing Telegram notification...
[TELEGRAM] 📤 Sending notification for visitor: ...
[TELEGRAM] ✅ Notification sent successfully
```

### 4.4. Проверка Telegram

Проверьте Telegram - должно прийти уведомление:
- 🆕 "Новый посетитель на сайте!" (при первом посещении)
- 🔄 "Посетитель вернулся на сайт!" (при последующих)

## 🐛 Решение проблем

### Проблема: Переменные окружения не работают

**Решение:**
1. Убедитесь, что файл называется именно `.env.local`
2. Перезапустите сервер после изменения `.env.local`
3. Проверьте, что нет пробелов вокруг `=`

### Проблема: Запросы не доходят до API

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что сервер запущен на `localhost:3000`
3. Проверьте, что нет проблем с CORS

### Проблема: Посетители определяются как боты

**Решение:**
1. Проверьте логи: `[TRACK-xxx] ❌ Request rejected: bot detected`
2. На localhost это нормально для некоторых случаев
3. Для тестирования можно временно отключить проверку ботов

### Проблема: Telegram не отправляет уведомления

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
2. Убедитесь, что токен валидный
3. Проверьте логи: `[TELEGRAM] ❌ Failed to send notification`

## 📊 Проверка сохраненных данных

После тестирования проверьте файл:
```
data/visitors.json
```

Должны быть сохранены данные о посетителях.

## ✅ Готово к деплою

Если все работает:
1. ✅ Тестовый endpoint отвечает
2. ✅ Логи показывают успешное отслеживание
3. ✅ Уведомления приходят в Telegram
4. ✅ Данные сохраняются

Можно пушить на GitHub! 🚀

