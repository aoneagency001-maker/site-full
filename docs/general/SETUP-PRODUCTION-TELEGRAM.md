# 🔧 Настройка Telegram на Production

## ⚠️ Проблема

При проверке `/api/telegram-check` вы видите:
```json
{
  "configured": false,
  "message": "❌ Telegram не настроен..."
}
```

Это значит, что переменные окружения **не установлены на production**.

---

## ✅ Решение: Настройка на Vercel

### Шаг 1: Получите токены

**TELEGRAM_BOT_TOKEN:**
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**TELEGRAM_CHAT_ID:**
1. Напишите вашему боту любое сообщение (например `/start`)
2. Запустите локально: `npx tsx scripts/get-telegram-chat-id.ts`
3. Скопируйте Chat ID (число, например: `123456789`)

### Шаг 2: Установите переменные на Vercel

1. Зайдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `site-full` (или ваш проект)
3. Откройте **Settings** → **Environment Variables**
4. Нажмите **Add New**
5. Добавьте переменные:

   **Переменная 1:**
   - Name: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен бота
   - Environment: выберите **все** (Production, Preview, Development)
   - Нажмите **Save**

   **Переменная 2:**
   - Name: `TELEGRAM_CHAT_ID`
   - Value: ваш Chat ID
   - Environment: выберите **все**
   - Нажмите **Save**

   **Переменная 3 (если еще не установлена):**
   - Name: `NEXT_PUBLIC_SITE_URL`
   - Value: `https://aoneagency.kz`
   - Environment: выберите **все**
   - Нажмите **Save**

### Шаг 3: Передеплойте проект

**ВАЖНО:** После добавления переменных нужно передеплоить!

1. В Vercel Dashboard откройте вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (⋮) → **Redeploy**
4. Подождите 2-3 минуты

### Шаг 4: Проверьте

1. Откройте: `https://aoneagency.kz/api/telegram-check`
2. Должно быть:
   ```json
   {
     "configured": true,
     "message": "✅ Telegram настроен правильно"
   }
   ```

3. Откройте сайт в режиме инкогнито
4. Должно прийти уведомление в Telegram

---

## ✅ Решение: Настройка на DigitalOcean

### Шаг 1: Получите токены (см. выше)

### Шаг 2: Установите переменные

1. Зайдите в [DigitalOcean Dashboard](https://cloud.digitalocean.com)
2. Apps → ваш проект
3. Settings → **App-Level Environment Variables**
4. Нажмите **Edit**
5. Добавьте переменные:
   - `TELEGRAM_BOT_TOKEN` = ваш токен
   - `TELEGRAM_CHAT_ID` = ваш Chat ID
   - `NEXT_PUBLIC_SITE_URL` = `https://aoneagency.kz`
6. Нажмите **Save**

### Шаг 3: Передеплойте

1. В Apps → ваш проект
2. Нажмите **Actions** → **Force Rebuild**
3. Подождите 5-10 минут

### Шаг 4: Проверьте (см. выше)

---

## 🔍 Проверка логов

После деплоя проверьте логи:

**Vercel:**
- Dashboard → проект → **Logs** → **Runtime Logs**
- Ищите: `[TELEGRAM] ✅ Notification sent successfully`

**DigitalOcean:**
- Apps → проект → **Runtime Logs**
- Ищите те же сообщения

---

## 🆘 Если не работает

1. **Проверьте формат токена:**
   - Должен быть: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
   - Должен начинаться с цифр и содержать двоеточие

2. **Проверьте Chat ID:**
   - Должно быть число (например: `123456789`)
   - Убедитесь, что вы написали боту хотя бы одно сообщение

3. **Проверьте логи:**
   - Ищите сообщения: `[TELEGRAM] ⚠️ WARNING: Telegram credentials not configured!`
   - Это значит переменные не установлены

4. **Убедитесь, что передеплоили:**
   - Переменные применяются только после передеплоя!

---

**Дата:** 20.11.2025


