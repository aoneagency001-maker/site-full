# 🔍 Диагностика системы отслеживания посетителей

## Проблема: Уведомления не приходят

### Шаг 1: Проверка в браузере (клиентская сторона)

1. Откройте сайт в браузере
2. Откройте консоль разработчика (F12)
3. Перейдите на вкладку "Console"
4. Найдите логи, начинающиеся с `[VISITOR-TRACKER]`

**Что должно быть:**
```
[VISITOR-TRACKER] 🚀 Starting tracking for pathname: /ru
[VISITOR-TRACKER] 📤 Sending tracking request: {page: "/ru", sessionId: "...", referrer: "..."}
[VISITOR-TRACKER] 📥 Response status: 200
[VISITOR-TRACKER] 📦 Response data: {tracked: true, visitorId: "..."}
[VISITOR-TRACKER] ✅ Visitor tracked successfully: ...
```

**Если видите ошибки:**
- `Failed to fetch` - проблема с сетью или API недоступен
- `404` - API route не найден
- `500` - ошибка на сервере

### Шаг 2: Проверка на сервере (логи)

Проверьте логи сервера на наличие:

**Успешный запрос:**
```
[TRACK-xxx] 📥 New tracking request received
[TRACK-xxx] ✅ Request passed validation, processing...
[TRACK-xxx] 💾 Saving visitor data: ...
[TRACK-xxx] ✅ Visitor data saved
[TRACK-xxx] 📤 Preparing Telegram notification...
[TELEGRAM] 📤 Sending notification for visitor: ...
[TELEGRAM] ✅ Notification sent successfully
```

**Если посетитель определяется как бот:**
```
[TRACK-xxx] ❌ Request rejected: bot detected
```

**Если IP невалидный:**
```
[TRACK-xxx] ❌ Request rejected: invalid IP
```

**Если проблема с Telegram:**
```
[TELEGRAM] ❌ Failed to send notification: ...
[TELEGRAM] Error description: ...
```

### Шаг 3: Проверка переменных окружения

Убедитесь, что установлены:
- `TELEGRAM_BOT_TOKEN` - токен бота
- `TELEGRAM_CHAT_ID` - ID чата

**Как проверить:**
1. В логах сервера должно быть:
   ```
   [TRACK-xxx] Telegram credentials check: {hasToken: true, hasChatId: true}
   ```

2. Если `hasToken: false` или `hasChatId: false` - переменные не установлены

### Шаг 4: Проверка базы данных

Система использует файловую систему для хранения данных:
- Файл: `data/visitors.json`
- Директория создается автоматически

**Проблемы:**
- На некоторых платформах (Vercel, некоторые серверы) файловая система может быть read-only
- Это НЕ должно блокировать отправку в Telegram (добавлена обработка ошибок)

**Проверка:**
- Если видите `[TRACK-xxx] ❌ Error saving data:` - проблема с файловой системой
- Но уведомления все равно должны отправляться

### Шаг 5: Проверка фильтрации ботов

Система фильтрует ботов по User-Agent. Если реальный посетитель определяется как бот:

**Решение:**
1. Проверьте логи: `[TRACK-xxx] ❌ Request rejected: bot detected`
2. Проверьте User-Agent в логах
3. Если это реальный браузер, но определяется как бот - нужно добавить исключение

### Шаг 6: Тестирование вручную

1. Откройте сайт в режиме инкогнито (чтобы это было новое посещение)
2. Откройте консоль браузера (F12)
3. Проверьте логи `[VISITOR-TRACKER]`
4. Проверьте логи сервера `[TRACK-xxx]` и `[TELEGRAM]`
5. Проверьте Telegram - должно прийти уведомление

### Шаг 7: Проверка с разных устройств

1. Откройте сайт с телефона (другая сеть)
2. Проверьте логи на сервере
3. Убедитесь, что запрос дошел до API

## Частые проблемы и решения

### Проблема 1: Запросы не доходят до API

**Признаки:**
- В консоли браузера: `Failed to fetch` или `Network error`
- Нет логов `[TRACK-xxx]` на сервере

**Решения:**
- Проверьте, что сайт работает
- Проверьте, что API route доступен: `https://ваш-сайт.com/api/track-visitor`
- Проверьте CORS настройки (если есть)

### Проблема 2: Посетители определяются как боты

**Признаки:**
- В логах: `[TRACK-xxx] ❌ Request rejected: bot detected`
- Уведомления не приходят

**Решения:**
- Проверьте User-Agent в логах
- Если это реальный браузер, но определяется как бот - проблема в списке ботов
- Можно временно отключить проверку для тестирования

### Проблема 3: Telegram не отправляет уведомления

**Признаки:**
- В логах: `[TELEGRAM] ❌ Failed to send notification`
- Или: `[TELEGRAM] Telegram credentials not configured`

**Решения:**
- Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
- Проверьте, что токен валидный
- Проверьте, что бот не заблокирован
- Проверьте, что Chat ID правильный

### Проблема 4: IP определяется как невалидный

**Признаки:**
- В логах: `[TRACK-xxx] ❌ Request rejected: invalid IP`

**Решения:**
- Это редко, так как проверка разрешает "unknown" IP
- Если проблема - можно временно отключить проверку

## Временное отключение проверок для тестирования

Если нужно быстро проверить, работает ли система:

1. Временно отключите проверку ботов (закомментируйте в `route.ts`)
2. Временно отключите проверку IP (закомментируйте в `route.ts`)
3. Проверьте, приходят ли уведомления
4. Если да - проблема в фильтрации
5. Если нет - проблема в другом месте

## Контакты для помощи

Если проблема не решена:
1. Соберите все логи из консоли браузера
2. Соберите все логи с сервера
3. Проверьте переменные окружения
4. Проверьте, что сайт работает и API доступен

