# 🚀 Пошаговая инструкция: Деплой aoneagency.kz на DigitalOcean

## ✅ Что нужно перед началом

- [x] Аккаунт DigitalOcean (у вас уже есть)
- [ ] GitHub репозиторий с кодом (или создадим)
- [ ] Домен aoneagency.kz (или настроим позже)
- [ ] 15-20 минут времени

---

## 🎯 Вариант 1: DigitalOcean App Platform (РЕКОМЕНДУЕТСЯ)

**Плюсы:** Автоматический деплой, SSL, масштабирование, простота  
**Минусы:** Чуть дороже (~$5-12/мес)

### Шаг 1: Подготовка кода

#### 1.1. Проверьте, что проект готов к деплою

```bash
# В папке проекта выполните:
npm install
npm run build
```

**Примечание:** Если сборка выдает ошибку подключения к Google Fonts — это нормально, на сервере DigitalOcean это будет работать. Главное, чтобы не было ошибок TypeScript или зависимостей.

Если сборка прошла успешно ✅ — продолжаем.

#### 1.2. Создайте GitHub репозиторий (если еще нет)

```bash
# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: aoneagency.kz ready for deploy"

# Создайте репозиторий на GitHub.com и выполните:
git remote add origin https://github.com/ВАШ-USERNAME/aoneagency-site.git
git branch -M main
git push -u origin main
```

**Важно:** Убедитесь, что файл `.env.local` НЕ попал в Git (он уже в `.gitignore`).

---

### Шаг 2: Создание App в DigitalOcean

#### 2.1. Войдите в DigitalOcean

1. Откройте [https://cloud.digitalocean.com](https://cloud.digitalocean.com)
2. Войдите в аккаунт

#### 2.2. Создайте новое приложение

1. Нажмите **"Apps"** в левом меню
2. Нажмите **"Create App"** (синяя кнопка)
3. Выберите **"GitHub"** как источник кода
4. Если первый раз — авторизуйте DigitalOcean доступ к GitHub
5. Выберите ваш репозиторий: `aoneagency-site`
6. Выберите ветку: **`main`**
7. Нажмите **"Next"**

#### 2.3. Настройте параметры сборки

DigitalOcean обычно определяет автоматически, но проверьте:

- **Type:** Web Service
- **Build Command:** `npm run build`
- **Run Command:** `npm start`
- **Environment:** Node.js
- **Node Version:** 18.x или 20.x

Нажмите **"Next"**

#### 2.4. Добавьте переменные окружения

В разделе **"Environment Variables"** добавьте:

```
NEXT_PUBLIC_SITE_URL=https://aoneagency.kz
```

**Остальные переменные (опционально, можно добавить позже):**

```
# Google Analytics 4 (если есть)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Yandex.Metrika (если есть)
NEXT_PUBLIC_YM_ID=12345678

# Telegram Bot (для уведомлений)
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id

# amoCRM (для контактной формы)
AMO_CRM_TOKEN=ваш_токен
AMO_CRM_SUBDOMAIN=ваш_поддомен

# Yandex IndexNow (для быстрой индексации)
INDEXNOW_KEY=ваш_ключ
```

**Важно:** 
- `NEXT_PUBLIC_*` переменные доступны в браузере
- Остальные переменные только на сервере
- Можно добавить позже в настройках App

Нажмите **"Next"**

#### 2.5. Выберите план

- **Basic Plan:** $5/мес (512 MB RAM) — для начала достаточно
- **Professional Plan:** $12/мес (1 GB RAM) — если ожидаете много трафика

Выберите план и нажмите **"Next"**

#### 2.6. Настройте домен (опционально)

Если домен готов:

1. В разделе **"Domains"** нажмите **"Add Domain"**
2. Введите: `aoneagency.kz`
3. DigitalOcean покажет DNS-записи, которые нужно добавить

**Настройка DNS у регистратора домена:**

Добавьте CNAME записи:
```
Type: CNAME
Name: @
Value: your-app-name.ondigitalocean.app
TTL: 3600

Type: CNAME
Name: www
Value: your-app-name.ondigitalocean.app
TTL: 3600
```

**Или A-запись (если CNAME не поддерживается):**
```
Type: A
Name: @
Value: IP-адрес (DigitalOcean покажет)
TTL: 3600
```

Если домена еще нет — можно добавить позже в настройках App.

#### 2.7. Запустите деплой

1. Нажмите **"Create Resources"** или **"Deploy"**
2. Дождитесь завершения (5-10 минут)
3. Следите за логами сборки в реальном времени

---

### Шаг 3: Проверка после деплоя

#### 3.1. Проверьте сайт

1. Откройте URL: `https://your-app-name.ondigitalocean.app`
2. Или ваш домен: `https://aoneagency.kz` (если настроили)

#### 3.2. Проверьте основные страницы

- [ ] Главная: `/`
- [ ] О нас: `/about`
- [ ] Блог: `/blog`
- [ ] Контакты: `/contacts`
- [ ] Услуги: `/targetirovannaya-reklama`, `/seo-prodvizhenie`, и т.д.

#### 3.3. Проверьте SSL

- [ ] В браузере должен быть зеленый замок 🔒
- [ ] URL начинается с `https://`

---

## 🖥️ Вариант 2: DigitalOcean Droplet (VPS)

**Плюсы:** Больше контроля, дешевле для высоких нагрузок  
**Минусы:** Требует настройки сервера вручную

### Шаг 1: Создание Droplet

1. В DigitalOcean Dashboard выберите **"Droplets"**
2. Нажмите **"Create Droplet"**
3. Настройки:
   - **Image:** Ubuntu 24.04 LTS
   - **Plan:** Basic ($6/мес, 1 GB RAM) или Regular ($12/мес, 2 GB RAM)
   - **Region:** Frankfurt (ближе к Казахстану) или Amsterdam
   - **Authentication:** SSH Key (рекомендуется) или Password
4. Нажмите **"Create Droplet"**
5. Дождитесь создания (1-2 минуты)
6. Скопируйте IP-адрес

### Шаг 2: Подключение к серверу

```bash
# Подключитесь к серверу
ssh root@ВАШ-IP-АДРЕС

# Или если используете SSH ключ
ssh -i ~/.ssh/your_key root@ВАШ-IP-АДРЕС
```

### Шаг 3: Установка Node.js и зависимостей

```bash
# Обновите систему
apt update && apt upgrade -y

# Установите Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Проверьте версию
node -v  # Должно быть v20.x.x
npm -v

# Установите PM2 (менеджер процессов)
npm install -g pm2

# Установите Nginx (веб-сервер)
apt install -y nginx
```

### Шаг 4: Загрузка проекта

#### Вариант A: Через Git (рекомендуется)

```bash
# Перейдите в директорию для сайтов
cd /var/www

# Клонируйте репозиторий
git clone https://github.com/ВАШ-USERNAME/aoneagency-site.git
cd aoneagency-site

# Установите зависимости
npm install

# Создайте файл с переменными окружения
nano .env.local
```

Вставьте в `.env.local`:
```env
NEXT_PUBLIC_SITE_URL=https://aoneagency.kz
# Добавьте остальные переменные при необходимости
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

```bash
# Соберите проект
npm run build
```

#### Вариант B: Через SCP (если нет Git)

```bash
# На вашем компьютере выполните:
scp -r /путь/к/проекту root@ВАШ-IP:/var/www/aoneagency-site
```

Затем на сервере:
```bash
cd /var/www/aoneagency-site
npm install
npm run build
```

### Шаг 5: Запуск приложения

```bash
# Запустите с PM2
pm2 start npm --name "aoneagency" -- start

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск при перезагрузке сервера
pm2 startup
# Выполните команду, которую покажет PM2

# Проверьте статус
pm2 status
pm2 logs aoneagency
```

### Шаг 6: Настройка Nginx

```bash
# Создайте конфигурацию
nano /etc/nginx/sites-available/aoneagency.kz
```

Вставьте:
```nginx
server {
    listen 80;
    server_name aoneagency.kz www.aoneagency.kz;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохраните и активируйте:
```bash
# Создайте символическую ссылку
ln -s /etc/nginx/sites-available/aoneagency.kz /etc/nginx/sites-enabled/

# Удалите дефолтную конфигурацию (опционально)
rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
nginx -t

# Перезапустите Nginx
systemctl restart nginx
```

### Шаг 7: Настройка SSL (Let's Encrypt)

```bash
# Установите Certbot
apt install -y certbot python3-certbot-nginx

# Получите SSL-сертификат
certbot --nginx -d aoneagency.kz -d www.aoneagency.kz

# Следуйте инструкциям:
# - Введите email
# - Согласитесь с условиями
# - Выберите редирект HTTP → HTTPS (рекомендуется: 2)
```

Certbot автоматически обновит конфигурацию Nginx и добавит SSL.

### Шаг 8: Настройка DNS

У вашего регистратора домена добавьте A-запись:

```
Type: A
Name: @
Value: IP-адрес вашего Droplet
TTL: 3600

Type: A
Name: www
Value: IP-адрес вашего Droplet
TTL: 3600
```

Подождите 5-15 минут для распространения DNS.

### Шаг 9: Проверка

1. Откройте `https://aoneagency.kz`
2. Проверьте SSL (зеленый замок)
3. Проверьте все страницы

---

## 🔄 Обновление сайта

### App Platform (автоматически)

```bash
# На вашем компьютере:
git add .
git commit -m "Обновление контента"
git push origin main

# DigitalOcean автоматически:
# 1. Заметит изменения
# 2. Запустит сборку
# 3. Задеплоит новую версию
```

### Droplet (вручную)

```bash
# Подключитесь к серверу
ssh root@ВАШ-IP

# Перейдите в директорию проекта
cd /var/www/aoneagency-site

# Обновите код
git pull origin main

# Установите новые зависимости (если есть)
npm install

# Пересоберите проект
npm run build

# Перезапустите приложение
pm2 restart aoneagency

# Проверьте логи
pm2 logs aoneagency
```

---

## 📊 После деплоя: SEO настройки

### 1. Google Search Console

1. Зайдите в [Google Search Console](https://search.google.com/search-console)
2. Добавьте сайт: `https://aoneagency.kz`
3. Подтвердите владение (через meta-тег или DNS)
4. Отправьте sitemap: `https://aoneagency.kz/sitemap.xml`

### 2. Yandex.Webmaster

1. Зайдите в [Yandex.Webmaster](https://webmaster.yandex.ru/)
2. Добавьте сайт: `https://aoneagency.kz`
3. Подтвердите владение
4. Отправьте sitemap: `https://aoneagency.kz/sitemap.xml`

### 3. Проверка индексации

Через 1-2 дня проверьте:
- Google: `site:aoneagency.kz`
- Yandex: `site:aoneagency.kz`

---

## 🛠️ Полезные команды (для Droplet)

```bash
# Статус PM2
pm2 status

# Логи приложения
pm2 logs aoneagency

# Перезапуск
pm2 restart aoneagency

# Остановка
pm2 stop aoneagency

# Статус Nginx
systemctl status nginx

# Перезапуск Nginx
systemctl restart nginx

# Проверка конфигурации Nginx
nginx -t

# Обновление SSL-сертификата (автоматически каждые 90 дней)
certbot renew --dry-run
```

---

## ✅ Чек-лист после деплоя

- [ ] Сайт открывается по адресу `https://aoneagency.kz`
- [ ] SSL-сертификат установлен (зеленый замок)
- [ ] Все страницы доступны и работают
- [ ] Формы отправляются (проверьте контактную форму)
- [ ] Изображения загружаются
- [ ] Мобильная версия работает
- [ ] `/sitemap.xml` доступен
- [ ] `/robots.txt` доступен
- [ ] Google Analytics работает (если настроен)
- [ ] Yandex.Metrika работает (если настроен)
- [ ] Отправлен sitemap в Google Search Console
- [ ] Отправлен sitemap в Yandex.Webmaster

---

## 🆘 Решение проблем

### Проблема: Сайт не открывается

**App Platform:**
1. Проверьте логи в разделе "Runtime Logs"
2. Проверьте статус деплоя в "Activity"
3. Убедитесь, что сборка прошла успешно

**Droplet:**
```bash
# Проверьте статус PM2
pm2 status

# Проверьте логи
pm2 logs aoneagency

# Проверьте, что приложение слушает порт 3000
netstat -tlnp | grep 3000

# Проверьте Nginx
systemctl status nginx
nginx -t
```

### Проблема: Ошибка сборки

1. Проверьте логи сборки
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Node.js (нужна 18+)

### Проблема: SSL не работает

**App Platform:**
- SSL настраивается автоматически
- Проверьте DNS-записи

**Droplet:**
```bash
# Проверьте сертификат
certbot certificates

# Обновите сертификат
certbot renew
```

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи (App Platform → Runtime Logs или `pm2 logs`)
2. Проверьте статус сервисов
3. Убедитесь, что все переменные окружения добавлены
4. Проверьте DNS-записи

---

**Готово! 🎉 Ваш сайт aoneagency.kz теперь в интернете!**

