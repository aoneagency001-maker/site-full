# ⚡ Быстрый деплой на DigitalOcean

## 🎯 Вариант 1: App Platform (5 минут)

### Шаги:

1. **GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Ready for deploy"
   git remote add origin https://github.com/ВАШ-USERNAME/aoneagency-site.git
   git push -u origin main
   ```

2. **DigitalOcean:**
   - Apps → Create App
   - Подключите GitHub репозиторий
   - Build: `npm run build`
   - Run: `npm start`
   - Добавьте переменную: `NEXT_PUBLIC_SITE_URL=https://aoneagency.kz`
   - Deploy

3. **Домен (опционально):**
   - Settings → Domains → Add Domain
   - Настройте DNS у регистратора

**Готово!** Сайт будет доступен через 5-10 минут.

---

## 🖥️ Вариант 2: Droplet (15 минут)

### Шаги:

1. **Создайте Droplet:**
   - Ubuntu 24.04, $6/мес, Frankfurt

2. **Подключитесь:**
   ```bash
   ssh root@IP-АДРЕС
   ```

3. **Установите:**
   ```bash
   apt update && apt upgrade -y
   curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
   apt install -y nodejs nginx
   npm install -g pm2
   ```

4. **Загрузите проект:**
   ```bash
   cd /var/www
   git clone https://github.com/ВАШ-USERNAME/aoneagency-site.git
   cd aoneagency-site
   npm install
   npm run build
   ```

5. **Запустите:**
   ```bash
   pm2 start npm --name "aoneagency" -- start
   pm2 save
   pm2 startup
   ```

6. **Настройте Nginx:**
   ```bash
   nano /etc/nginx/sites-available/aoneagency.kz
   ```
   Вставьте:
   ```nginx
   server {
       listen 80;
       server_name aoneagency.kz www.aoneagency.kz;
       location / {
           proxy_pass http://localhost:3000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```
   ```bash
   ln -s /etc/nginx/sites-available/aoneagency.kz /etc/nginx/sites-enabled/
   nginx -t
   systemctl restart nginx
   ```

7. **SSL:**
   ```bash
   apt install -y certbot python3-certbot-nginx
   certbot --nginx -d aoneagency.kz -d www.aoneagency.kz
   ```

**Готово!** Настройте DNS и подождите 5-15 минут.

---

## 📋 Переменные окружения

Добавьте в DigitalOcean App Platform или в `.env.local` на сервере:

```
NEXT_PUBLIC_SITE_URL=https://aoneagency.kz
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX (опционально)
NEXT_PUBLIC_YM_ID=12345678 (опционально)
TELEGRAM_BOT_TOKEN=... (опционально)
TELEGRAM_CHAT_ID=... (опционально)
```

---

## ✅ После деплоя

1. Проверьте: `https://aoneagency.kz`
2. Отправьте sitemap в Google Search Console
3. Отправьте sitemap в Yandex.Webmaster

**Подробная инструкция:** См. `v04_2025-11-20_10-29-23_DEPLOY-INSTRUCTION.md`

