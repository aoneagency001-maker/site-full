# 📊 Интеграция с Yandex.Metrika API

## Описание

Система для получения данных из Yandex.Metrika и отправки отчетов в Telegram.

## 🎯 Какие данные можно получать

### Основные метрики:
- ✅ **Визиты** (`ym:s:visits`) - количество визитов
- ✅ **Пользователи** (`ym:s:users`) - уникальные посетители
- ✅ **Просмотры** (`ym:s:pageviews`) - просмотры страниц
- ✅ **Показатель отказов** (`ym:s:bounceRate`) - процент отказов
- ✅ **Средняя длительность визита** (`ym:s:avgVisitDurationSeconds`)

### Дополнительные метрики:
- `ym:s:newUsers` - новые пользователи
- `ym:s:newUsersPercentage` - процент новых пользователей
- `ym:s:pageDepth` - глубина просмотра
- `ym:s:goal<ID>reaches` - достижения целей
- `ym:s:sumParams` - сумма параметров

### Группировки (dimensions):
- `ym:s:date` - по датам
- `ym:s:trafficSource` - по источникам трафика
- `ym:s:searchEngine` - по поисковым системам
- `ym:s:regionCountry` - по странам
- `ym:s:regionCity` - по городам
- `ym:s:deviceCategory` - по устройствам (desktop/mobile/tablet)
- `ym:s:browser` - по браузерам
- `ym:s:operatingSystem` - по ОС
- `ym:pv:URL` - по страницам

## 🔧 Настройка

### 1. Получение OAuth токена

1. Зайдите на [Yandex OAuth](https://oauth.yandex.ru/)
2. Создайте новое приложение или используйте существующее
3. Получите OAuth токен
4. Добавьте в `.env.local`:

```env
YANDEX_METRIKA_OAUTH_TOKEN=ваш_oauth_токен
NEXT_PUBLIC_YM_ID=ваш_id_счетчика
```

### 2. Получение ID счетчика

ID счетчика - это число, которое вы видите в URL Metrika:
- `https://metrika.yandex.ru/dashboard?id=12345678` → ID = `12345678`

Или в настройках счетчика в интерфейсе Metrika.

## 📡 API Endpoints

### GET `/api/analytics/yandex`

Получение данных из Yandex.Metrika.

**Параметры запроса:**
- `date1` (опционально) - начальная дата (формат: YYYY-MM-DD)
- `date2` (опционально) - конечная дата (формат: YYYY-MM-DD)
- `metrics` (опционально) - метрики через запятую (по умолчанию: основные метрики)
- `dimensions` (опционально) - группировки через запятую

**Пример запроса:**
```bash
GET /api/analytics/yandex?date1=2025-01-01&date2=2025-01-31&metrics=ym:s:visits,ym:s:users
```

**Ответ:**
```json
{
  "success": true,
  "counterId": "12345678",
  "period": {
    "date1": "2025-01-01",
    "date2": "2025-01-31"
  },
  "metrics": ["ym:s:visits", "ym:s:users"],
  "totals": [1500, 1200],
  "data": [...]
}
```

### POST `/api/analytics/yandex`

Получение данных и отправка отчета в Telegram.

**Тело запроса:**
```json
{
  "date1": "2025-01-01",
  "date2": "2025-01-31",
  "metrics": ["ym:s:visits", "ym:s:users"],
  "sendToTelegram": true
}
```

**Ответ:**
```json
{
  "success": true,
  "report": "📊 Отчет Yandex.Metrika...",
  "data": {...}
}
```

## 📊 Примеры использования

### 1. Получение статистики за сегодня

```typescript
const response = await fetch('/api/analytics/yandex');
const data = await response.json();
console.log('Визиты:', data.totals[0]);
```

### 2. Получение статистики по источникам трафика

```typescript
const response = await fetch(
  '/api/analytics/yandex?dimensions=ym:s:trafficSource&metrics=ym:s:visits'
);
const data = await response.json();
// data.data содержит данные по каждому источнику
```

### 3. Отправка отчета в Telegram

```typescript
const response = await fetch('/api/analytics/yandex', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    date1: '2025-01-01',
    date2: '2025-01-31',
    sendToTelegram: true,
  }),
});
```

## 🔄 Автоматические отчеты

Можно настроить автоматическую отправку отчетов через cron или Vercel Cron:

### Vercel Cron (vercel.json):

```json
{
  "crons": [
    {
      "path": "/api/analytics/yandex",
      "schedule": "0 9 * * *"
    }
  ]
}
```

### Node.js cron:

```typescript
import cron from 'node-cron';

// Ежедневно в 9:00
cron.schedule('0 9 * * *', async () => {
  await fetch('http://localhost:3000/api/analytics/yandex', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ sendToTelegram: true }),
  });
});
```

## 📱 Формат отчета в Telegram

```
📊 Отчет Yandex.Metrika

📅 Период: 01.01.2025 - 31.01.2025

📈 Основные метрики:
👥 Визиты: 1,500
👤 Пользователи: 1,200
📄 Просмотры: 3,800
⏱ Средняя длительность: 2 мин 15 сек
📉 Показатель отказов: 45.2%

🔗 Открыть в Metrika
```

## 🎯 Полезные метрики для бизнеса

### Для маркетинга:
- `ym:s:visits` - общий трафик
- `ym:s:newUsers` - прирост аудитории
- `ym:s:goal<ID>reaches` - конверсии по целям
- `ym:s:trafficSource` - эффективность каналов

### Для SEO:
- `ym:s:searchEngine` - трафик из поисковых систем
- `ym:s:searchPhrase` - поисковые запросы
- `ym:s:pageDepth` - глубина просмотра

### Для конверсий:
- `ym:s:goal<ID>reaches` - достижения целей
- `ym:s:ecommerce<ID>purchaseRevenue` - доход от покупок
- `ym:s:bounceRate` - качество трафика

## 🔗 Полезные ссылки

- [Yandex.Metrika API Documentation](https://yandex.ru/dev/metrika/doc/api2/api_v1/intro-docpage/)
- [Список всех метрик](https://yandex.ru/dev/metrika/doc/api2/api_v1/metrics-docpage/)
- [Список всех группировок](https://yandex.ru/dev/metrika/doc/api2/api_v1/dimensions-docpage/)
- [OAuth токен](https://oauth.yandex.ru/)

## ⚠️ Ограничения API

- **Лимит запросов:** 10,000 запросов в день
- **Глубина данных:** до 2 лет назад
- **Размер ответа:** до 1,000,000 строк
- **Требуется OAuth токен** для доступа

## ✅ Чек-лист настройки

- [ ] Получен OAuth токен из Yandex OAuth
- [ ] Добавлен `YANDEX_METRIKA_OAUTH_TOKEN` в `.env.local`
- [ ] Добавлен `NEXT_PUBLIC_YM_ID` в `.env.local`
- [ ] Протестирован запрос `GET /api/analytics/yandex`
- [ ] Протестирована отправка отчета в Telegram
- [ ] Настроены автоматические отчеты (опционально)

---

**Готово!** Теперь вы можете получать данные из Yandex.Metrika и отправлять отчеты в Telegram! 🚀

