# 🚀 Инструкция по деплою на DigitalOcean

## Подготовка к деплою

### 1. Переменные окружения

Создайте файл `.env.local` в корне проекта:

```env
# Site
NEXT_PUBLIC_SITE_URL=https://aoneagency.kz

# Analytics
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_YM_ID=12345678

# CRM (amoCRM)
AMO_CRM_TOKEN=your_token_here
AMO_CRM_SUBDOMAIN=yourcompany

# IndexNow (Yandex)
INDEXNOW_KEY=your_key_here

# Email (для формы обратной связи)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_password
```

### 2. Сборка проекта

```bash
npm install
npm run build
```

Проверьте, что сборка прошла успешно без ошибок.

## Деплой на DigitalOcean App Platform

### Вариант 1: Через DigitalOcean App Platform (рекомендуется)

1. **Подготовьте репозиторий:**
   - Загрузите код в GitHub/GitLab/Bitbucket
   - Убедитесь, что `.env.local` добавлен в `.gitignore`

2. **Создайте App в DigitalOcean:**
   - Зайдите в DigitalOcean Dashboard
   - Выберите "Apps" → "Create App"
   - Подключите ваш репозиторий

3. **Настройки сборки:**
   - **Build Command:** `npm run build`
   - **Run Command:** `npm start`
   - **Environment:** Node.js
   - **Node Version:** 18.x или выше

4. **Добавьте переменные окружения:**
   - В настройках App добавьте все переменные из `.env.local`
   - Убедитесь, что `NEXT_PUBLIC_*` переменные доступны

5. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения деплоя
   - Ваш сайт будет доступен по адресу `https://your-app-name.ondigitalocean.app`

### Вариант 2: На VPS (Droplet)

1. **Подключитесь к серверу:**
   ```bash
   ssh root@your-server-ip
   ```

2. **Установите Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. **Установите PM2 (менеджер процессов):**
   ```bash
   sudo npm install -g pm2
   ```

4. **Клонируйте репозиторий:**
   ```bash
   git clone your-repo-url
   cd agency-kit-site
   ```

5. **Установите зависимости и соберите:**
   ```bash
   npm install
   npm run build
   ```

6. **Создайте файл `.env.local`** с переменными окружения

7. **Запустите с PM2:**
   ```bash
   pm2 start npm --name "aoneagency" -- start
   pm2 save
   pm2 startup
   ```

8. **Настройте Nginx (опционально):**
   ```nginx
   server {
       listen 80;
       server_name aoneagency.kz;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

9. **Настройте SSL (Let's Encrypt):**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d aoneagency.kz
   ```

## Проверка после деплоя

- [ ] Сайт открывается по домену
- [ ] Все страницы загружаются корректно
- [ ] Формы работают (если настроены)
- [ ] Изображения загружаются
- [ ] Проверьте `/robots.txt` и `/sitemap.xml`
- [ ] Проверьте мета-теги в исходном коде страниц
- [ ] Запустите Lighthouse для проверки производительности

## Обновление сайта

После изменений в коде:

```bash
git pull
npm install
npm run build
pm2 restart aoneagency  # если используете PM2
```

Или если используете App Platform - просто сделайте push в репозиторий, деплой произойдет автоматически.

## Мониторинг

- **PM2:** `pm2 status`, `pm2 logs aoneagency`
- **DigitalOcean App Platform:** логи доступны в Dashboard
- **Google Search Console:** добавьте сайт для мониторинга индексации
- **Yandex.Webmaster:** добавьте сайт для мониторинга в Яндексе

## Полезные команды

```bash
# Проверка статуса PM2
pm2 status

# Просмотр логов
pm2 logs aoneagency

# Перезапуск
pm2 restart aoneagency

# Остановка
pm2 stop aoneagency

# Удаление из автозапуска
pm2 delete aoneagency
```

