# 🎯 Гайд по конверсионным элементам сайта

## ✅ Что добавлено

### 1. **Интерактивный квиз "Узнайте стоимость таргета за 60 секунд"**

**Где:** Главная страница (после секции "Услуги")

**Как работает:**
1. Пользователь кликает на кнопку "Узнать стоимость за 60 секунд"
2. Открывается модальное окно с квизом
3. Проходит 5 шагов:
   - Цель (Заявки/Продажи/Бренд/Трафик)
   - Ниша бизнеса
   - Бюджет на рекламу
   - Платформы (множественный выбор)
   - Контактные данные (Имя + Телефон + Email)
4. Данные сохраняются в `/data/quiz-submissions.json`
5. Пользователь перенаправляется в WhatsApp с персональным сообщением

**Файлы:**
- `components/quiz/TargetQuiz.tsx` — компонент квиза
- `components/quiz/QuizModal.tsx` — модальное окно
- `components/landing/QuizSection.tsx` — секция на главной
- `app/api/quiz/route.ts` — API для сохранения данных

---

### 2. **Психологические триггеры**

#### **FOMO (Страх упущенной выгоды)**
**Где:** Правый верхний угол (фиксированный)
```
"Только 3 слота на этой неделе" (с пульсирующим индикатором)
```

#### **Социальное доказательство**
**Где:** Левый нижний угол (появляется через 5 секунд)
```
"Айгуль из Алматы только что заказала таргет в Instagram"
```

**Файл:** `components/landing/TriggersSection.tsx`

---

### 3. **Улучшенные кейсы**

**Где:** Главная страница (заменили старую секцию CaseStudiesSection)

**Что нового:**
- ✅ Метрики "До → После" с процентами (+400%, -70%, etc.)
- ✅ Градиентный header с ключевыми цифрами
- ✅ Структура "Проблема → Решение → Результат"
- ✅ Отзывы клиентов в каждом кейсе

**Кейсы:**
1. Салон красоты: Рост заявок в 5 раз (Instagram)
2. Интернет-магазин: Выход в ТОП-3 Google за 45 дней (SEO)

**Файл:** `components/landing/ImprovedCaseStudiesSection.tsx`

---

## 📊 База данных квиз-заявок

### Где хранятся данные:
```
/data/quiz-submissions.json
```

### Формат данных:
```json
{
  "id": "1700000000000",
  "goal": "leads",
  "niche": "local",
  "budget": "100-300k",
  "platforms": ["instagram", "facebook"],
  "name": "Айгуль",
  "phone": "+7 747 385 4493",
  "email": "aigul@example.kz",
  "type": "target-quiz",
  "createdAt": "2025-01-20T12:00:00.000Z",
  "submittedAt": "2025-01-20T12:00:00.000Z",
  "ip": "185.123.45.67",
  "userAgent": "Mozilla/5.0..."
}
```

### Просмотр заявок:
```bash
# Открыть файл
cat data/quiz-submissions.json

# Или через API
curl http://localhost:3000/api/quiz
```

---

## 🔧 API Endpoints

### POST /api/quiz
Сохранение новой заявки из квиза.

**Request:**
```json
{
  "goal": "leads",
  "niche": "local",
  "budget": "100-300k",
  "platforms": ["instagram"],
  "name": "Айгуль",
  "phone": "+7 747 385 4493",
  "email": "aigul@example.kz"
}
```

**Response (200):**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена",
  "id": "1700000000000"
}
```

### GET /api/quiz
Получение всех заявок (для админки).

**Response (200):**
```json
{
  "success": true,
  "count": 15,
  "submissions": [...]
}
```

---

## 🚀 Как тестировать

### 1. Запустить dev сервер
```bash
npm run dev
```

### 2. Открыть сайт
```
http://localhost:3000
```

### 3. Протестировать квиз
1. Кликнуть "Узнать стоимость за 60 секунд" (Hero или QuizSection)
2. Пройти все шаги
3. Заполнить контакты (Имя + Телефон обязательны)
4. Нажать "Получить расчёт в WhatsApp"
5. Проверить, что откры

лся WhatsApp с сообщением
6. Проверить `/data/quiz-submissions.json` — должна появиться новая заявка

### 4. Проверить триггеры
- FOMO badge (правый верхний угол) — должен быть виден сразу
- Social Proof (левый нижний угол) — появляется через 5 секунд

### 5. Проверить кейсы
- Скроллить до секции кейсов
- Проверить анимацию появления
- Кликнуть на отзывы

---

## 📈 Метрики конверсии

### Что отслеживать:

1. **Конверсия в квиз:**
   - Клики на кнопку "Узнать стоимость"
   - Открытие квиза
   - Переходы между шагами
   - Заполнение контактов

2. **Конверсия в WhatsApp:**
   - Клики на финальную кнопку
   - Переход в WhatsApp

3. **Показатели:**
   - CTR (клики на кнопку / показы страницы)
   - Completion Rate (заполнение квиза / открытие квиза)
   - WhatsApp Rate (переход в WhatsApp / заполнение квиза)

### Целевая конверсия:
- **Hero → Квиз:** 15-25%
- **Квиз → Заполнение:** 60-75%
- **Заполнение → WhatsApp:** 90-95%

---

## 🔮 Будущие улучшения

### 1. Интеграция с amoCRM
```typescript
// В /app/api/quiz/route.ts добавить:
async function sendToAmoCRM(data: any) {
  await fetch('https://yourcompany.amocrm.ru/api/v4/leads', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${process.env.AMO_CRM_TOKEN}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      name: `Квиз: ${data.name}`,
      price: calculatePrice(data.budget),
      custom_fields_values: [
        { field_id: 123, values: [{ value: data.phone }] },
        { field_id: 456, values: [{ value: data.goal }] },
      ],
    }),
  });
}
```

### 2. A/B тестирование квиза
- Разные формулировки вопросов
- Разные цвета кнопок
- Разные офферы

### 3. Дополнительные квизы
- "Калькулятор стоимости разработки приложения"
- "Подберите идеальную услугу для вашего бизнеса"
- "Аудит рекламы за 5 минут"

### 4. Email-последовательность
- Отправка расчёта на email
- Напоминания через 1 день, 3 дня, 7 дней

---

## ⚠️ Важно

### Безопасность данных:
- ✅ `/data/` добавлен в `.gitignore` — данные клиентов не попадут в Git
- ✅ Валидация телефона и email на фронте
- ✅ IP и User Agent сохраняются для защиты от спама

### Резервное копирование:
```bash
# Создать бэкап
cp data/quiz-submissions.json data/quiz-submissions-backup-$(date +%Y%m%d).json

# Восстановить из бэкапа
cp data/quiz-submissions-backup-20250120.json data/quiz-submissions.json
```

### Очистка данных:
```bash
# Удалить все заявки (осторожно!)
echo "[]" > data/quiz-submissions.json
```

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи API: `npm run dev` (в терминале будут ошибки)
2. Проверьте файл: `/data/quiz-submissions.json`
3. Проверьте права доступа: `ls -la data/`

---

**Готово! 🎉 Квиз работает и сохраняет данные!**

**Следующий шаг:** Добавьте Google Analytics событие для отслеживания конверсий:
```javascript
gtag('event', 'quiz_completed', {
  'event_category': 'engagement',
  'event_label': 'target_quiz',
  'value': data.budget
});
```

