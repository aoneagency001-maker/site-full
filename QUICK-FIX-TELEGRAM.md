# 🚨 СРОЧНО: Настройка Telegram на Production

## ❌ Проблема

После деплоя `/api/telegram-check` показывает:
```json
{
  "configured": false,
  "hasToken": false,
  "hasChatId": false
}
```

**Это значит: переменные окружения НЕ установлены на Vercel!**

---

## ✅ РЕШЕНИЕ (5 минут)

### Шаг 1: Получите токены

**TELEGRAM_BOT_TOKEN:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**TELEGRAM_CHAT_ID:**
1. Напишите вашему боту `/start`
2. Запустите локально: `npx tsx scripts/get-telegram-chat-id.ts`
3. Скопируйте Chat ID (число, например: `123456789`)

### Шаг 2: Установите на Vercel

1. **Откройте:** https://vercel.com/dashboard
2. **Выберите проект:** `site-full` (или ваш проект)
3. **Settings** → **Environment Variables**
4. **Add New** → Добавьте:

   **Переменная 1:**
   ```
   Name: TELEGRAM_BOT_TOKEN
   Value: [ваш токен]
   Environment: ☑ Production ☑ Preview ☑ Development
   ```

   **Переменная 2:**
   ```
   Name: TELEGRAM_CHAT_ID
   Value: [ваш Chat ID]
   Environment: ☑ Production ☑ Preview ☑ Development
   ```

   **Переменная 3 (если нет):**
   ```
   Name: NEXT_PUBLIC_SITE_URL
   Value: https://aoneagency.kz
   Environment: ☑ Production ☑ Preview ☑ Development
   ```

5. **Сохраните** каждую переменную

### Шаг 3: ⚠️ ВАЖНО - Передеплойте!

**Без передеплоя переменные НЕ применятся!**

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите **⋮** (три точки) → **Redeploy**
4. Подождите 2-3 минуты

### Шаг 4: Проверьте

1. Откройте: `https://aoneagency.kz/api/telegram-check`
2. Должно быть:
   ```json
   {
     "configured": true,
     "message": "Telegram configured correctly"
   }
   ```

3. Откройте сайт в режиме инкогнито
4. Должно прийти уведомление в Telegram! ✅

---

## 🔍 Проверка логов

После деплоя проверьте логи:

1. Vercel Dashboard → проект → **Logs** → **Runtime Logs**
2. Ищите: `[TELEGRAM] ✅ Notification sent successfully`

Если видите `[TELEGRAM] ⚠️ WARNING: Telegram credentials not configured!` → переменные не установлены!

---

## 🆘 Если не работает

1. **Убедитесь, что передеплоили** (самая частая ошибка!)
2. Проверьте формат токена: `1234567890:ABC...` (цифры:буквы)
3. Проверьте Chat ID: должно быть число
4. Убедитесь, что написали боту хотя бы одно сообщение

---

**Время настройки: 5 минут** ⏱️

