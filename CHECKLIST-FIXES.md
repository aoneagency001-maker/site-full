# ✅ Чеклист исправлений для продакшена

## 🔍 Проверенные и исправленные проблемы

### 1. ✅ Обработка ошибок сохранения файлов
- **Проблема**: На некоторых платформах (Vercel, DigitalOcean App Platform) файловая система может быть read-only
- **Исправление**: Добавлена обработка ошибок в `saveData()` для contact и quiz API
- **Результат**: Теперь даже если сохранение в файл не работает, отправка в Telegram продолжает работать

### 2. ✅ Пути API
- **Проверено**: Все формы используют относительные пути `/api/contact` и `/api/quiz`
- **Статус**: ✅ Правильно - работают с любым доменом

### 3. ✅ Отправка в Telegram
- **Проверено**: Все API routes используют `sendToTelegram()` из `lib/telegram.ts`
- **Статус**: ✅ Работает для:
  - Форм контактов (`/api/contact`)
  - Квизов (`/api/quiz`)
  - Отслеживания посетителей (`/api/track-visitor`)

### 4. ✅ Кнопки и формы
- **Проверено**: Все кнопки используют правильные обработчики
- **Статус**: ✅ Работают:
  - Кнопки открытия квиза (`onClick={() => setIsQuizOpen(true)}`)
  - Кнопки навигации
  - Кнопки отправки форм
  - Ссылки на WhatsApp и телефон

## 🚨 Возможные проблемы на продакшене

### Проблема 1: Переменные окружения не установлены
**Симптомы**: Формы отправляются, но уведомления в Telegram не приходят

**Решение**:
1. Проверьте, что на сервере установлены:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
2. Перезапустите сервер после изменения переменных

### Проблема 2: API routes не работают
**Симптомы**: Формы не отправляются, ошибки в консоли браузера

**Решение**:
1. Проверьте, что API routes доступны:
   - `https://ваш-сайт.com/api/contact`
   - `https://ваш-сайт.com/api/quiz`
   - `https://ваш-сайт.com/api/track-visitor/test`
2. Проверьте логи сервера на наличие ошибок

### Проблема 3: JavaScript не загружается
**Симптомы**: Кнопки не работают, формы не отправляются

**Решение**:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что все компоненты правильно импортированы
3. Проверьте, что Next.js правильно собирает проект

### Проблема 4: VisitorTracker не инициализируется
**Симптомы**: Отслеживание посетителей не работает

**Решение**:
1. Проверьте, что компонент подключен в `app/[locale]/layout.tsx`
2. Проверьте консоль браузера на наличие ошибок `[VISITOR-TRACKER]`
3. Убедитесь, что нет конфликтов с другими скриптами

## 📋 Чеклист для проверки на продакшене

### Формы
- [ ] Форма контактов на главной странице отправляется
- [ ] Форма контактов на странице `/contacts` отправляется
- [ ] Квиз отправляется и открывается WhatsApp
- [ ] После отправки формы приходит уведомление в Telegram

### Кнопки
- [ ] Кнопка "Пройти квиз" открывает модальное окно
- [ ] Кнопки навигации работают
- [ ] Ссылки на WhatsApp открываются
- [ ] Ссылки на телефон работают

### Отслеживание
- [ ] При загрузке страницы отправляется запрос на `/api/track-visitor`
- [ ] В Telegram приходят уведомления о посетителях
- [ ] Клики по CTA кнопкам отслеживаются

### API
- [ ] `/api/contact` отвечает на POST запросы
- [ ] `/api/quiz` отвечает на POST запросы
- [ ] `/api/track-visitor` отвечает на POST запросы
- [ ] `/api/track-visitor/test` показывает статус Telegram

## 🔧 Быстрая диагностика

### Проверка 1: Тестовый endpoint
Откройте: `https://ваш-сайт.com/api/track-visitor/test`

Должно показать:
```json
{
  "status": "ok",
  "telegram": {
    "hasToken": true,
    "hasChatId": true
  }
}
```

### Проверка 2: Консоль браузера
Откройте сайт и нажмите F12 → Console

Должны быть логи:
- `[VISITOR-TRACKER] 🚀 Starting tracking`
- `[VISITOR-TRACKER] ✅ Visitor tracked successfully`

### Проверка 3: Логи сервера
Проверьте логи на наличие:
- `[TRACK-xxx]` - отслеживание посетителей
- `[TELEGRAM]` - отправка уведомлений
- Ошибок при обработке форм

## ✅ Все исправления применены

1. ✅ Обработка ошибок сохранения файлов
2. ✅ Улучшенное логирование
3. ✅ Проверка путей API
4. ✅ Проверка всех форм и кнопок

Теперь все должно работать на продакшене!

